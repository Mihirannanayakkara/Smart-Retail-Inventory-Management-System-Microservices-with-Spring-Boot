package com.smartretail.order.repository;

/**
 * Deprecated: OrderItems are embedded within the Order document in MongoDB.
 * No separate repository is needed. This stub exists to avoid JPA import
 * errors.
 */
public interface OrderItemRepository {
}
